# Exercise 1

You can also think of insertion sort as a recursive algorithm. In order to sort A[1:n] , recursively sort the subarray A[1:n-1] and then insert A[n] into the sorted subarray. Write pseudocode for this recursive version of insertion sort.


